name: creapp
services:
  creappapi:
    container_name: creappapi
    build:
      context: ./
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - /root/backup/creapp/api-data:/app/uploads
    ports:
      - "5002:5000"
    depends_on:
      - postgres
    networks:
      - caddy-net
  
  postgres:
    container_name: creapp_postgres
    image: postgres:16
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - /root/backup/creapp/postgres-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - caddy-net

networks:
  caddy-net:
    driver: bridge
    external: true